server:
  port: 8081

spring:
  application:
    name: organization-service

  datasource:
    url: jdbc:postgresql://localhost:5432/hrm_organization
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:9090/realms/hrm-realm

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka